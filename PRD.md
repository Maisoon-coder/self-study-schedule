# 个人工作台 APP · 产品需求文档（PRD）

> 版本：v1.0（第一版基线）
> 日期：2026-08-29
> 状态：需求已确认，进入设计/开发依据文档
> 适用范围：仅作者本人使用，单设备本地运行

---

## 1. 产品目标和使用场景

### 1.1 产品目标
做一个**只给作者自己用的个人工作台网页**，把日常工作、学习、备考相关的记录集中在一个界面里管理，替代零散的便签、备忘录和多个网站。核心诉求是"打开就能用、数据不丢、随手记随手看"。

### 1.2 关键约束（硬要求）
- **单 HTML 文件**：整个 APP 是一个本地 HTML 文件，双击或在浏览器打开即用，无需安装、无需部署服务器。
- **纯本地存储**：所有数据存在当前设备的浏览器 `localStorage` 中，**刷新、关闭浏览器、重启电脑后数据不丢失**。
- **不登录、不联网、不用云数据库、不部署、不做手机端自动同步**。
- **手机可用（手动搬运）**：PC 与手机各存一份数据、互不自动同步；换设备看数据用"导出 JSON → 导入 JSON"手动搬。

### 1.3 使用场景
- **电脑前**：日常主用，录入和管理各模块数据。
- **手机上**：偶尔查看，或少量补充；通过把 PC 导出的数据文件传到手机、手机导入后查看同样内容。
- **典型一天**：早上打开首页看今日计划和各模块提醒 → 在学校事务/算法题/六级里推进 → 完成的勾掉 → 定期导出一份数据备份。

---

## 2. 整体页面框架和导航结构

### 2.1 应用形态
单页应用（SPA），无路由框架，原生 JS 切换"页面"（用一个 state 控制当前显示哪个 section）。

### 2.2 页面清单（共 6 个）
1. 首页总览
2. 今日计划
3. 算法题
4. 六级备考
5. 学校事务
6. 数据与设置

### 2.3 导航结构
- **PC 宽屏**：左侧固定导航栏，6 个页面入口，当前页高亮。
- **手机窄屏**：底部标签栏（tab bar），6 个入口，一眼可达（已确认采用底部标签栏）。
- 首页是唯一**聚合页**，其余为各自功能页。

### 2.4 技术边界
- 原生 HTML + CSS + JavaScript，无构建步骤、无第三方框架依赖（如确需图标可用内联 SVG）。
- 所有渲染与状态管理在前端完成，无任何网络请求。
- 单设备 `localStorage` 约 5MB 上限，纯文本数据足够。

---

## 3. 首页总览的内容结构

首页是"一眼看全局"的聚合页，自上而下：

1. **顶部**：当前日期 + 问候语。
2. **快速备忘**：一个随手记文本框（纯文本），输入即存，跨设备不自动同步。
3. **今日计划摘要**：今日计划里"未完成"的任务列表（默认显示未开始/进行中），含状态、优先级、分类、来源标记；可快速勾完成。
4. **各模块关键摘要卡**（默认以下指标，后续可调整）：
   - 算法题：连续打卡天数、今日是否已打卡。
   - 六级备考：连续打卡天数、今日是否已打卡。
   - 学校事务：未来 N 天内即将到期的作业/竞赛/活动/通知条数（按提前天数口径显示"即将到期"）。
   - 算法题/六级/学校事务的条目总数概览。
5. **空态引导**：某模块无数据时显示"去添加"入口，跳转到对应页。

---

## 4. 每个板块的用途

### 4.1 首页总览
聚合展示今日计划、快速备忘、各模块关键指标与提醒，是每天打开的第一个页面。

### 4.2 今日计划
纯待办清单（**不排具体时间段/钟点**）。每条任务带：状态（未开始 / 进行中 / 已完成）、优先级（高 / 中 / 低）、分类、来源（手动添加 / 由模块派来）、重复属性（无 / 每天）。可接收其他模块派来的任务，支持每天重复的固定任务。

### 4.3 算法题
刷算法题的进度管理。拆成 **4 个独立子板块**：LeetCode、牛客、洛谷、蓝桥官网。每道题记录：题号、难度（简单/中等/困难）、题型标签、掌握程度、刷题状态。可把某题派任务到今日计划。有**连续打卡天数**。
- 不做：解法笔记、错题复习机制。

### 4.4 六级备考
英语六级备考管理，含：
- **真题/模考**子板块：记录成绩（听力错多少、阅读错多少）、用时、弱项。
- **写作/翻译**子板块：存模板和范文（纯文本）。
- **单词、听力**：可派任务到今日计划（具体计划形式后续定，v1 先支持添加条目 + 一键派发）。
- 有**连续打卡天数**。

### 4.5 学校事务
记录学校里的事务，分 4 类：学科作业、竞赛日期、活动会议、学院通知。每条带：DDL（截止/发生日期）、状态、注意事项。用户给每类设一个**提前天数**，系统在到期前按提前天数自动把任务派到今日计划。

### 4.6 数据与设置
数据备份与全局设置中枢：
- 导出全部数据为 JSON 文件（下载）。
- 导入 JSON 文件恢复数据（二次确认后覆盖）。
- 浅色 / 深色主题切换。
- 存储信息展示：各模块数据条数、占用估算、说明"数据存在浏览器本地 localStorage"。
- 清空：按模块清空 + 全部清空，均需二次确认防误删。
- **不做自动备份**（已确认）。

---

## 5. 每个板块的第一版最必要的数据操作

### 5.1 今日计划
- 新增任务（标题、状态默认未开始、优先级、分类、重复属性）。
- 编辑 / 删除任务。
- 切换状态（未开始 ↔ 进行中 ↔ 已完成）。
- 标记重复（每天）。
- 接收并显示"由模块派来"的任务（带来源标记）。
- 顶部筛选（按状态 / 优先级 / 分类，至少支持状态筛选）。

### 5.2 算法题
- 4 个子板块切换。
- 每板块内：新增 / 编辑 / 删除题目（题号、难度、题型标签、掌握程度、刷题状态）。
- 单题"派任务到今日计划"（生成一条来源=派的任务，源题标记"已派"，不重复派）。
- 顶部"今日打卡"按钮 + 显示连续打卡天数。

### 5.3 六级备考
- **真题/模考**：新增 / 编辑 / 删除记录（日期、听力错几、阅读错几、用时、弱项标签）。
- **写作/翻译**：新增 / 编辑 / 删除模板或范文（类型写作/翻译、标题、内容，纯文本）。
- **单词/听力**：添加条目 + 一键"派任务到今日计划"。
- 顶部"今日打卡"按钮 + 显示连续打卡天数。

### 5.4 学校事务
- 4 类切换（学科作业 / 竞赛日期 / 活动会议 / 学院通知）。
- 每类：新增 / 编辑 / 删除条目（标题、DDL、状态、注意事项、提前天数）。
- 设置每类的"提前天数"。
- 打开 APP / 首页时，对"今天 ≥ DDL − 提前天数 且未派过"的条目自动派任务到今日计划并标记"已派"，不重复建。

### 5.5 数据与设置
- 导出全部数据 → 下载 JSON。
- 导入 JSON → 二次确认 → 覆盖恢复。
- 主题切换（浅色 / 深色），即时生效并持久化。
- 显示各模块数据条数与占用估算。
- 按模块清空（二次确认）。
- 全部清空（二次确认）。

### 5.6 首页 / 快速备忘
- 快速备忘：文本输入即存（localStorage）。
- 今日计划未完成摘要读取（只读展示，可勾完成回写今日计划）。
- 各模块摘要读取（只读展示）。

---

## 6. 首页、今日计划和各板块之间的关系

- **今日计划是任务收口**：算法题、六级（单词/听力）、学校事务都可把条目"派"成今日计划里的一条任务；派发为**单向**（勾完成只改今日计划，不回写源条目）。
- **首页是聚合查看层**：读取今日计划（未完成）与各模块摘要（打卡天数、即将到期），本身不产出业务数据，仅"快速备忘"产生数据。
- **学校事务自动派发**：不依赖手动点"派"，系统在首页/打开时按提前天数自动建今日计划任务。
- **关系图（逻辑）**：
  ```
  算法题 ──派任务──┐
  六级(单词/听力)─派任务─┤
  学校事务(自动)──派任务─┼──▶ 今日计划 ◀──▶ 首页(摘要)
  手动添加 ──────────────┘
  ```
- **打卡**：算法题与六级各自独立计连续天数，互不干扰，首页分别展示。

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 存储位置
- 运行态数据：**浏览器 `localStorage`**（key 建议 `personal_workbench_v1` 存整体 JSON）。
- 每次数据变更**即时写入** localStorage，保证刷新/关闭/重启不丢。

### 7.2 数据格式
- 内存与导出共用同一份 JSON 结构（见数据结构草表），导出文件即该 JSON 的序列化。

### 7.3 备份与恢复（手动搬运，PC ↔ 手机）
- **导出**：在"数据与设置"点导出，浏览器下载一个 `workbench-data-YYYYMMDD.json`。
- **导入**：选本地 JSON 文件 → **二次确认弹窗** → 覆盖当前数据并刷新界面。
- 这是跨设备搬数据的**唯一通道**（已确认不做自动备份、不做联网同步）。

### 7.4 丢失风险与说明
- 清浏览器缓存 / 换浏览器 / 换设备未导入 → 数据丢失，需在"数据与设置"里明确提示用户定期导出。
- 单设备约 5MB 上限，纯文本足够，无需压缩。

---

## 8. 第一版必须完成的功能

- [ ] 单 HTML 文件骨架 + 6 个页面切换 + 浅色/深色主题。
- [ ] **今日计划**：增删改、状态切换（未开始/进行中/已完成）、优先级、分类、重复任务（每天）、手动加任务、状态筛选。
- [ ] **算法题**：4 子板块；题目增删改（题号/难度/题型标签/掌握程度/刷题状态）；派任务到今日计划；连续打卡天数 + 今日打卡。
- [ ] **六级备考**：真题模考（成绩/用时/弱项）增删改；写作翻译（模板范文）增删改；单词听力派任务到今日计划；连续打卡天数 + 今日打卡。
- [ ] **学校事务**：4 类增删改（DDL/状态/注意事项）；每类提前天数设置；到期前自动派任务到今日计划。
- [ ] **首页总览**：今日计划未完成摘要 + 快速备忘 + 各模块关键摘要（打卡天数、即将到期提醒）。
- [ ] **数据与设置**：导出全部 JSON / 导入恢复 / 主题切换 / 存储信息展示 / 按模块清空 + 全部清空（二次确认）。
- [ ] 所有数据落 localStorage，刷新/关闭/重启不丢。
- [ ] 空态引导（各列表无数据时显示添加入口）。
- [ ] 必填校验（标题/题号/DDL 等缺失禁止保存并提示）。

---

## 9. 第一版暂时不做的功能

- 自动备份（用户明确不要）。
- 算法题的解法笔记、错题复习机制（用户明确不要）。
- 手机自动同步（仅做手动导出/导入）。
- 图表 / 趋势可视化（体重、成绩趋势等，先文字，后续再加）。
- 浏览器通知 / 弹窗提醒（靠首页与今日计划的"提前派任务"代替）。
- 富文本编辑（模板/范文先用纯文本，够用再说）。
- 搜索、高级筛选（先保证基础增删改通顺）。
- 多用户 / 登录 / 云存储 / 部署。

---

## 10. 可以实际检查的验收标准（Acceptance Criteria）

以下每条都应能在浏览器中实际验证：

**A. 运行与持久化**
- A1：双击 HTML 文件在浏览器打开，6 个页面均可切换，无 JS 报错。
- A2：在任意模块新增一条数据 → 刷新页面（F5）→ 数据仍在。
- A3：关闭浏览器标签 / 重启浏览器 / 重启电脑 → 重新打开 HTML，数据仍在。

**B. 今日计划**
- B1：可新增任务并指定状态、优先级、分类、重复（每天）。
- B2：切换状态在 未开始 / 进行中 / 已完成 间正确生效并持久化。
- B3：标记"每天"的重复任务在次日打开时仍出现在今日计划。
- B4：可按状态筛选查看。

**C. 算法题**
- C1：可在 LeetCode / 牛客 / 洛谷 / 蓝桥 四个子板块各自增删改题目，字段（题号/难度/标签/掌握度/状态）齐全。
- C2：点"派任务"后，今日计划出现一条来源=派的任务，源题显示"已派"；再次点不重复派。
- C3：点"今日打卡"后连续天数 +1 并显示；断签一天再打卡天数重置为 1。

**D. 六级备考**
- D1：真题模考记录含 听力错几/阅读错几/用时/弱项，可增删改。
- D2：写作/翻译模板范文可增删改（纯文本）。
- D3：单词/听力条目可一键派到今日计划。
- D4：连续打卡天数与今日打卡逻辑同 C3。

**E. 学校事务**
- E1：4 类条目可增删改，含 DDL、状态、注意事项、提前天数。
- E2：设置提前天数 N 后，当"今天 ≥ DDL − N"时，该条目自动出现在今日计划并标记"已派"，且不重复建。
- E3：已派条目在今日计划勾完成后，源条目仍保留"已派"标记（单向）。

**F. 首页**
- F1：首页显示今日计划的未完成任务，可勾完成并回写。
- F2：首页显示算法题、六级的连续打卡天数与"今日是否已打卡"。
- F3：首页显示学校事务未来即将到期条目数（按提前天数口径）。
- F4：快速备忘输入后持久化，刷新不丢。

**G. 数据与设置**
- G1：导出生成 JSON 文件并成功下载。
- G2：导入该 JSON → 二次确认 → 数据被正确恢复，界面刷新。
- G3：浅色/深色切换即时生效并持久化（刷新后保持）。
- G4：显示各模块数据条数与占用估算。
- G5：按模块清空、全部清空均弹出二次确认，确认后才执行。

**H. 跨设备（手动搬运）**
- H1：PC 导出 JSON → 拷到手机 → 手机导入 → 手机显示与 PC 相同数据。
- H2：手机新增数据后导出 → PC 导入 → PC 显示手机新增内容（覆盖式）。

---

## 附录：数据结构草表（v1）

| 实体 | 关键字段 |
|---|---|
| 今日计划任务 | id, 标题, 状态, 优先级, 分类, 来源(手动/派), 重复(无/每天), 创建日 |
| 算法题 | 平台, 题号, 难度, 题型标签[], 掌握程度, 刷题状态, 已派(bool) |
| 六级·模考 | 日期, 听力错, 阅读错, 用时, 弱项[] |
| 六级·模板范文 | 类型(写作/翻译), 标题, 内容 |
| 六级·单词听力 | 条目, 已派(bool) |
| 学校事务 | 类型, 标题, DDL, 状态, 注意事项, 提前天数, 已派(bool) |
| 打卡 | 算法题连续天数/末次日, 六级连续天数/末次日 |
| 快速备忘 | 文本 |
| 设置 | 主题(浅色/深色) |

## 附录：已确认的两处交互决定
1. 手机导航采用**底部标签栏**（6 入口一眼可达）。
2. 打卡断签：昨天打卡、今天还没打卡时，首页显示的连续天数**保持昨天的数字**（今天仍可补）；今天打卡后接续；断了一整天再打卡则重置为 1。
